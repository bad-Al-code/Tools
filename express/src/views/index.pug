extends layout.pug

block content
  h1 Dashboard Data
  table
    thead
      tr
        th ID
        th Name
        th Value
    tbody
      each item in data
        tr
          td= item.id
          td= item.name
          td= item.value
